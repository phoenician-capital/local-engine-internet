# local-engine-internet

Internet search layer for local LLMs at Phoenician Capital.

Local models cannot search the web on their own. This repo will give them a dedicated search engine so company apps and agents can query the internet reliably.

Implementation is in progress.
